[project]
name = "knoll2013-workspace"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "knoll-tools",
]

[tool.uv.workspace]
members = ["tools"]

[tool.uv.sources]
knoll-tools = { workspace = true }

[English](reproduction.md) | [日本語](reproduction.ja.md)

# 再現

## 本 scaffold が Knoll & van Dick (2013) Study 1 / 2 の数値とどう対応するか

完全な再現対象表は Obsidian 設計ノート（`Do I hear the whistle…` §5）にある．以下は本 scaffold が *再現できる/できない* もののサマリ．

| # | Knoll Study | 指標 | 論文値 | 本 scaffold で再現？ | 機構 |
|---|-------------|------|--------|---------------------|------|
| 1 | Study 1 N=373 | CFA χ²(48) | 182.87 | **Phase B3（繰延）** | 12 項目 reflexive self-rating 出力が必要 |
| 2 | Study 1 | CFA CFI (M3) | .95 | Phase B3（繰延） | 同上 |
| 3 | Study 1 | CFA CFI (M1, 1 因子) | .53 | Phase B3（繰延） | 同上 |
| 4 | Study 1 | CFA CFI (M2, 無相関 4 因子) | .85 | Phase B3（繰延） | 同上 |
| 5 | Study 2 N=184 | Cronbach α (AS=.88 / QS=.89 / PS=.82 / OS=.80) | ≥ .80 | ABM 側は Phase B3（繰延）；**Track A `reliability-analysis` は実データ・合成データのいずれでも今すぐ動作** | knoll-tools |
| 6 | Study 2 Table 2 | corr(AS, climate) = .65 | ≥ .50 符号 | **部分的** | `correlations.csv` の `AS × climate_of_silence` 行（最終ステップ） |
| 7 | Study 2 Table 2 | **corr(PS, climate) = .11 (n.s.)** | 95% CI が 0 を含む | **機構レベルで再現可能** | `--prosocial-climate-decoupling` が β_ρ^{PS} = 0 を強制；`visualize-sweep` の PS-decoupling 応答曲線参照 |
| 8 | Study 2 Table 2 | corr(QS, climate) ≈ .40 | ±.10 符号 | 部分的 | `correlations.csv` の `QS × climate_of_silence` |
| 9 | Study 2 Table 2 | corr(OS, climate) ≈ .35 | ±.10 符号 | 部分的 | `correlations.csv` の `OS × climate_of_silence` |

"部分的" の意味: rule モード ABM は正しい符号方向の Pearson r を生成するが，その大きさは選んだ β 群に依存し，Phase B3 / Phase X 統合なしでは Knoll の特定値に較正されない．

## 今すぐできること

1. **rule モード実行** と動機 mix 時系列の確認:
   ```bash
   cargo run --release -- run --decision-mode rule \
       --n-teams 8 --team-size 12 \
       --prosocial-climate-decoupling --t-max 36 --seed 42
   uv run knoll-tools visualize
   ```
   `motive_mix_timeseries.png` を見る．PS 比率が ≈ 0.40（Knoll Study 2 周辺値）に収束するか？

2. **β スイープ実行** で PS-風土独立性を確認:
   ```bash
   cargo run --release -- sweep --beta-rho-ps-values "0.0,0.1,0.3" --runs 10 --seed 42
   uv run knoll-tools visualize-sweep
   ```
   `sweep_corr_ps_climate.png` を見る．`decoupling=True` で PS-風土 r が β_ρ^{PS} 全域で Knoll target .11 線をなぞるべき．

3. **LLM モード実行**（Ollama 必須）:
   ```bash
   ollama pull llama3.1
   export OLLAMA_HOST=http://localhost:11434 OLLAMA_MODEL=llama3.1
   cargo run --release -- run --decision-mode llm --llm-cache-path runs/knoll_cache.json --t-max 24 --seed 42
   ```
   warm cache では再実行は瞬時で bit 完全一致．

4. **Track A 合成データ・サニティチェック**:
   ```bash
   uv run knoll-tools survey-loader --synthesize-n 200 --sample synth
   uv run knoll-tools reliability-analysis --sample synth
   uv run knoll-tools cfa-competing-models --sample synth --models M1,M2,M3,M3b,M4
   uv run knoll-tools nomological-network --sample synth --bootstrap 500
   ```
   Knoll Study 2 周辺値に較正した合成データでは，下位尺度別 α ≈ .80–.86，M3（相関 4 因子）が M1/M2 を ΔCFI ≥ .15 で凌駕，Knoll Table 2 論文値との符号一致率 > 90% が見えるはず．

## 繰延項目

- **Phase B3** — reflexive 12 項目 self-rating 出力（`knoll reproduce --emit-self-ratings`）；`knoll-tools cfa-analysis` で semopy 集団 CFA fit；χ² / CFI / RMSEA / α を Knoll Study 1 target と比較．
- **Phase X** — `knoll-tools reproduce-paper` による「論文 / Track A / Track B」三者比較．前提：Track A 実データ + Phase B3．
- **Phase A1–A5** — 実際の調査票確定，IRB 承認，データ収集，多群不変性．`survey/` ディレクトリはプレースホルダのみ．

---
*This file was generated by Claude Code.*
